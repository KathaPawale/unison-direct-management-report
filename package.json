{
  "name": "unison-direct-management-report",
  "version": "1.0.0",
  "private": true,
  "description": "Unison Direct GCC INC management reporting web application",
  "scripts": {
    "dev": "npx serve .",
    "start": "npx serve .",
    "test": "for t in tests/verify-*.js; do node $t || exit 1; done"
  },
  "devDependencies": {
    "playwright-core": "1.56.0",
    "serve": "^14.2.4",
    "xlsx-js-style": "1.2.0"
  }
}
