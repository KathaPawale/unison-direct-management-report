// Excel workbook parsing is kept in app.js so the uploaded prototype behavior remains unchanged.
export function workbookModuleInfo(){ return 'Excel processing preserved from uploaded Unison Direct prototype'; }
