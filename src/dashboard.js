// Dashboard calculations/rendering are kept in app.js so the exact uploaded UI behavior remains unchanged.
export function dashboardModuleInfo(){ return 'Dashboard behavior preserved from uploaded Unison Direct prototype'; }
