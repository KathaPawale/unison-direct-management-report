// PDF generation/download is kept in app.js so the exact uploaded prototype behavior remains unchanged.
export function pdfModuleInfo(){ return 'PDF behavior preserved from uploaded Unison Direct prototype'; }
